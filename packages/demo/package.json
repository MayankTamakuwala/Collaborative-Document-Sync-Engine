{
  "name": "@collab/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --sourcemap --outfile=public/main.js",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@collab/client": "0.1.0",
    "@collab/crdt": "0.1.0",
    "@collab/protocol": "0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
